spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pmtool
    username: postgres
    password: 363kyP98!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://alex-kanban.ca.auth0.com/
          audiences: https://pmtool.api 

server:
  port: 8080

#auth0:
#  audience: https://pmtool.api 
